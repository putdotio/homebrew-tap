# homebrew-tap

Homebrew tap for put.io command-line tools.

## Install

```bash
brew tap putdotio/tap
brew install putio
```

Formulae in this repository are updated automatically from release automation in the source project repositories.
